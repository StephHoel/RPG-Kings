# Migração de Enums + Refatoração do uso

## Schemas

Races
raceName: string
defaultAnimal?: string | null
stats: {
strength: number
agility: number
intelligence: number
charisma: number
stamina: number
}

Animals
animalName: string
race: string
stats: {
strength: number
agility: number
intelligence: number
charisma: number
stamina: number
}

Skills
skillName: string
type: enum<develop|fixed>
races: string[]

Disciplines
disciplineName: string
type: enum<mandatory|optional>
skils: string[]
stats: string[]

## Constantes

const disciplines: string[]
const skills: string[]
const animals: string[]
const races: string[]
const weekdays: string[]
const logType: string[]

## Versão 3 do BD

```ts
// src/db/index.ts - incluir aos schemas já existentes
this.version(3).stores({
  races: 'raceName,defaultAnimal,stats',
  animals: 'animalName,race,stats',
  skills: 'skillName,type,races',
  disciplines: 'disciplineName,type,skils,stats',
})
```

## Plano de ação

1. Criar schemas
2. Exportar schemas como type
3. Criar tabelas no banco
4. Criar consts
5. Criar seeds
6. Criar helper para bulk dos membros que não existirem
7. Adicionar seed ao populate com uso do helper criado
