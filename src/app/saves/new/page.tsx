import SavesNewClient from '@/components/pages/SavesNewClient'
import { metadatas } from '@/config/metadatas'

export const metadata = metadatas.saves_new

export default function SaveNew() {
  return <SavesNewClient />
}
